import styled from "styled-components";

export const SideBarLevelTitle = styled.div`
    user-select: none;
    cursor: pointer;

    h3 {
        display: inline-block;
        position: relative;
        top: -4px;
        margin: 5px 0 0 0;
    }
`;

export const SideBarLevelContent = styled.div`
    padding: 0 0 0 17px;
`;
