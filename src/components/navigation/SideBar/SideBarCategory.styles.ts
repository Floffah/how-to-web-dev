import styled from "styled-components";

export const SideBarCategoryTitle = styled.div`
    user-select: none;
    cursor: pointer;

    h3 {
        display: inline-block;
        position: relative;
        top: -4px;
        margin: 3px 0 0 0;
    }
`;

export const SideBarCategoryContent = styled.div`
    padding: 0 0 0 25px;
`;
