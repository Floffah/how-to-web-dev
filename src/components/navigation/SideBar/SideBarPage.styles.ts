import styled from "styled-components";

export const SideBarPageTitle = styled.div`
    user-select: none;
    cursor: pointer;

    h4 {
        display: inline-block;
        position: relative;
        top: -4px;
        margin: 5px 0 0 2px;
    }
`;
